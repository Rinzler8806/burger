<div class="container">
<background-image></background-image>
<h1>Cure that burger craving!</h1>

<h2>Burgers that are tasty!</h2>
<div class="col-3" id="burgers-list">
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

 <div class="col-3" id="devoured-list">
<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
  </div>

<h2>Add Burgers</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Send to Tasty Burger List
  </div>

  <button type="submit">Submit</button>
</form>>

</div>